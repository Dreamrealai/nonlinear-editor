# =============================================================================
# DreamReal AI - Environment Variables Configuration
# =============================================================================
#
# This file documents all environment variables used in the application.
# Copy this file to .env.local and fill in your actual values.
#
# SECURITY NOTES:
# - Never commit .env.local to version control
# - Service role keys and secret keys must be kept private
# - Only NEXT_PUBLIC_* variables are exposed to the browser
#
# =============================================================================

# =============================================================================
# Core Application Configuration
# =============================================================================

# Node environment (auto-set by Next.js, usually don't need to set manually)
# NODE_ENV=development

# Base URL of your application (used for redirects, webhooks, and OAuth callbacks)
# Required in production for Stripe webhooks and OAuth
# Local development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Alternative app URL (if different from BASE_URL)
# NEXT_PUBLIC_APP_URL=

# =============================================================================
# Supabase Configuration (REQUIRED)
# =============================================================================
# Get these from: https://app.supabase.com/project/_/settings/api

# Your Supabase project URL (format: https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anonymous key (public, safe to expose - respects RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase service role key (PRIVATE - bypasses RLS, never expose to client!)
# Required for admin operations, webhooks, and background tasks
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Management API access token (optional, for programmatic management)
# Get from: https://app.supabase.com/account/tokens
# SUPABASE_ACCESS_TOKEN=

# =============================================================================
# Stripe Configuration (REQUIRED for payments)
# =============================================================================
# Get these from: https://dashboard.stripe.com/apikeys

# Stripe secret key (PRIVATE - starts with sk_test_ or sk_live_)
STRIPE_SECRET_KEY=sk_test_...

# Stripe publishable key (PUBLIC - starts with pk_test_ or pk_live_)
# Optional: Only needed if using Stripe.js on the client side
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe webhook signing secret (PRIVATE - starts with whsec_)
# CRITICAL: REQUIRED for webhook security
# Get from: https://dashboard.stripe.com/webhooks
#
# For local development:
#   1. Install Stripe CLI: https://stripe.com/docs/stripe-cli
#   2. Run: stripe listen --forward-to localhost:3000/api/stripe/webhook
#   3. Copy the webhook signing secret (starts with whsec_)
#
# For production:
#   1. Add webhook endpoint at: https://dashboard.stripe.com/webhooks
#   2. Set URL to: https://your-domain.com/api/stripe/webhook
#   3. Select events: checkout.session.completed, customer.subscription.updated, customer.subscription.deleted
#   4. Copy the webhook signing secret
#
# WARNING: Webhooks will fail with 503 error if this is not configured
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Premium subscription price ID (starts with price_)
# Create in Stripe Dashboard or run: npm run setup:stripe
# This is the monthly recurring price for Premium tier ($49/month)
STRIPE_PREMIUM_PRICE_ID=price_...

# =============================================================================
# Google Cloud & AI Services
# =============================================================================

# Google Cloud Service Account JSON (for Vertex AI, Veo, Imagen, GCS, Video Intelligence)
# Get from: Google Cloud Console > IAM & Admin > Service Accounts
# Required permissions: Vertex AI User, Storage Admin, Video Intelligence API User
# Format: Entire JSON object as a single line
# GOOGLE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"..."}

# Google AI Studio API key (for Gemini chat)
# Get from: https://aistudio.google.com/app/apikey
# Recommended over GEMINI_API_KEY as it supports "latest" model aliases
# AISTUDIO_API_KEY=AIza...

# Google Gemini API key (alternative to AISTUDIO_API_KEY)
# GEMINI_API_KEY=AIza...

# Google Cloud Storage bucket name (for video processing)
# Auto-created if not specified using format: {project_id}-video-processing
# Must be globally unique, lowercase, alphanumeric with hyphens
# GCS_BUCKET_NAME=my-project-video-processing

# =============================================================================
# Video Generation & Processing APIs
# =============================================================================

# Fal.ai API key (for video upscaling and audio generation)
# Get from: https://fal.ai/dashboard/keys
# FAL_API_KEY=...

# Replicate API key (for AI model hosting, currently unused)
# Get from: https://replicate.com/account/api-tokens
# REPLICATE_API_KEY=...

# Segmind API key (for image generation, currently unused)
# Get from: https://www.segmind.com/
# SEGMIND_API_KEY=...

# Piapi API key (currently unused)
# PIAPI_API_KEY=...

# Shotstack API key (for video editing API, currently unused)
# Get from: https://shotstack.io/
# SHOTSTACK_API_KEY=...

# =============================================================================
# Audio Generation APIs
# =============================================================================

# ElevenLabs API key (for text-to-speech and sound effects)
# Get from: https://elevenlabs.io/app/settings/api-keys
# ELEVENLABS_API_KEY=...

# Comet API key (for Suno music generation via Comet API)
# Get from: https://api.comet.com/
# COMET_API_KEY=...

# Alternative Comet/Suno API key
# COMETSUNO_API_KEY=...

# Wavespeed API key (for audio processing)
# WAVESPEED_API_KEY=...

# =============================================================================
# LLM & Chat APIs
# =============================================================================

# OpenAI API key (if using OpenAI services like GPT-4, DALL-E)
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Anthropic API key (if using Claude API)
# Get from: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-...

# Perplexity API key (currently unused)
# Get from: https://www.perplexity.ai/
# PERPLEXITY_API_KEY=...

# =============================================================================
# Logging & Monitoring
# =============================================================================

# Axiom API token (for centralized logging and monitoring)
# Get from: https://app.axiom.co/settings/tokens
# AXIOM_TOKEN=xaat-...

# Axiom dataset name (where logs are stored)
# Create in Axiom dashboard or it will be auto-created
# AXIOM_DATASET=genai-video-production

# =============================================================================
# Email Services
# =============================================================================

# Resend API key (for transactional emails)
# Get from: https://resend.com/api-keys
# RESEND_API_KEY=re_...

# =============================================================================
# Development & Deployment
# =============================================================================

# Vercel API token (for deployment automation and preview deployments)
# Get from: https://vercel.com/account/tokens
# VERCEL_TOKEN=...

# CI environment indicator (automatically set by CI systems)
# CI=true

# Base URL for Playwright E2E tests (defaults to http://localhost:3000)
# BASE_URL=http://localhost:3000

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. REQUIRED FOR BASIC FUNCTIONALITY:
#    - Copy this file to .env.local
#    - Fill in NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY
#    - Fill in SUPABASE_SERVICE_ROLE_KEY
#    - Fill in STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, and STRIPE_PREMIUM_PRICE_ID
#
# 2. REQUIRED FOR AI FEATURES:
#    - Add at least one of: AISTUDIO_API_KEY, GEMINI_API_KEY, or GOOGLE_SERVICE_ACCOUNT
#    - For video generation (Veo): Requires GOOGLE_SERVICE_ACCOUNT
#    - For image generation (Imagen): Requires GOOGLE_SERVICE_ACCOUNT
#    - For chat (Gemini): Can use AISTUDIO_API_KEY or GOOGLE_SERVICE_ACCOUNT
#
# 3. OPTIONAL FEATURES:
#    - Video upscaling: FAL_API_KEY
#    - Text-to-speech: ELEVENLABS_API_KEY
#    - Music generation: COMET_API_KEY
#    - Logging/monitoring: AXIOM_TOKEN and AXIOM_DATASET
#
# 4. VALIDATION:
#    - Run: npm run validate:env
#    - Or check: npm run dev (validates on startup in development)
#
# =============================================================================
